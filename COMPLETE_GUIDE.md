# 🎉 G2A Market GUI - Полное руководство

## ✨ Что было добавлено из g2a-automation

### 1. 🎯 Точечное управление автоизменением

**Как работает:**
- Каждый оффер можно включить/выключить отдельно
- Правый клик на оффер → "🟢 Enable Auto-Price" или "🔴 Disable Auto-Price"
- Столбец "Auto" показывает статус: 🟢 ON / 🔴 OFF

**Новая логика:**
```python
# Белый список: included_products
# Черный список: excluded_products

Если глобально включено:
    Если в черном списке → ВЫКЛ
    Если есть белый список:
        Если в белом → ВКЛ
        Иначе → ВЫКЛ
    Иначе → ВКЛ (для всех)
```

### 2. 💰 Новая логика автоизменения: +0.05€

**Старая логика (было):**
- Если твоя цена > мин. конкурента → снижай на 0.01€
- Если ты единственный → повышай

**Новая логика (стало):**
```python
Твоя_цена = мин_конкурента + 0.05€

Пример:
- Мин. конкурент: €5.00
- Твоя цена станет: €5.05

Защита:
- Если ты единственный продавец → не меняем цену
- protect_single_seller: true (по умолчанию)
```

**Настройки:**
- `competitor_offset`: 0.05 (можно изменить)
- `protect_single_seller`: true/false
- `min_price`: 0.1
- `max_price`: 100.0
- `daily_limit`: 30

### 3. 📊 Статистика изменений цен

**Что сохраняется:**
```json
{
  "timestamp": "2025-12-01T01:00:00",
  "product_id": "abc123",
  "game_name": "Game Name",
  "old_price": 5.00,
  "new_price": 5.05,
  "market_price": 5.00,
  "change": 0.05,
  "change_percent": 1.0,
  "reason": "автоизменение" // или "manual"
}
```

**Где хранится:**
- Файл: `price_changes_stats.json`
- Автоматически сохраняется при каждом изменении

**Просмотр статистики:**
- За сегодня: 🔥 Today
- За 7 дней: 📆 Last 7 Days
- За 30 дней: 📅 Last 30 Days

**Что показывает:**
- Общее количество изменений
- Повышения/понижения цен
- Среднее изменение
- Топ-20 игр с наибольшими изменениями
- Последние 50 изменений

---

## 📚 Структура проекта

```
G2A_market_GUI/
├── gui/
│   ├── tabs/
│   │   ├── dashboard_tab.py          # Главная панель (автообновление)
│   │   ├── offers_tab_full.py         # Полная вкладка офферов (НОВОЕ!)
│   │   ├── offers_tab_improved.py    # Улучшенная версия
│   │   ├── statistics_tab.py         # Статистика (НОВОЕ!)
│   │   ├── keys_tab.py
│   │   ├── parsing_tab.py
│   │   └── settings_tab.py
│   └── styles.py
├── database.py                     # Старая версия
├── database_improved.py            # Новая с статистикой (НОВОЕ!)
├── g2a_api_client.py
├── gui_main.py
├── auto_price_settings.json        # Настройки автоизменения
├── price_changes_stats.json        # Статистика изменений (НОВОЕ!)
└── FEATURES.md
```

---

## 🚀 Как использовать новые возможности

### 1. Интегрировать новые файлы

#### Шаг 1: Обновите `gui_main.py`

```python
# В начале файла
from gui.tabs.offers_tab_full import OffersTabFull  # Новый импорт
from gui.tabs.statistics_tab import StatisticsTab    # Новый импорт

# В методе create_tabs()
class MainWindow(QMainWindow):
    def create_tabs(self):
        # ...
        
        # Замените старую offers_tab на новую
        self.offers_tab = OffersTabFull()  # НОВОЕ!
        self.tabs.addTab(self.offers_tab, "📦 Offers")
        
        # Добавьте статистику
        self.statistics_tab = StatisticsTab()  # НОВОЕ!
        self.tabs.addTab(self.statistics_tab, "📊 Statistics")
```

#### Шаг 2: Используйте новую Database

```python
# Вместо:
from database import Database

# Используйте:
from database_improved import DatabaseImproved as Database
```

### 2. Использование точечного автоизменения

1. **Откройте вкладку 📦 Offers**
2. **Нажмите 🔄 Refresh** чтобы загрузить офферы
3. **Правый клик на оффер** → выберите "🟢 Enable Auto-Price"
4. **Столбец "Auto"** покажет 🟢 ON

**Важно:**
- Глобально автоизменение должно быть включено
- При первом включении предложит включить глобально

### 3. Просмотр статистики

1. **Откройте вкладку 📊 Statistics**
2. **Выберите период:**
   - 🔥 Today - за сегодня
   - 📆 Last 7 Days - за неделю
   - 📅 Last 30 Days - за месяц

**Что увидите:**
- 📊 Общую сводку
- 🎮 Топ-20 игр с наибольшими изменениями
- 🕒 Последние 50 изменений

---

## ⚙️ Настройки автоизменения

### Файл: `auto_price_settings.json`

```json
{
  "enabled": false,                    // Глобальный выключатель
  "check_interval": 1800,              // 30 минут
  "competitor_offset": 0.05,           // +0.05€ от мин. конкурента
  "min_price": 0.1,                    // Мин. цена
  "max_price": 100.0,                  // Макс. цена
  "daily_limit": 30,                   // Макс. изменений в день
  "excluded_products": [],             // Черный список (Product ID)
  "included_products": [],             // Белый список (Product ID)
  "protect_single_seller": true        // Защита если ты один
}
```

### Пример использования:

**Сценарий 1: Автоизменение для всех**
```json
{
  "enabled": true,
  "excluded_products": [],  // Пустой
  "included_products": []   // Пустой
}
```
→ ВСЕ офферы будут автоматически менять цену

**Сценарий 2: Точечное управление**
```json
{
  "enabled": true,
  "excluded_products": [],
  "included_products": ["product_id_1", "product_id_2"]  // Только эти
}
```
→ Только указанные офферы будут менять цену

**Сценарий 3: Исключения**
```json
{
  "enabled": true,
  "excluded_products": ["product_id_3"],  // Не менять цену
  "included_products": []
}
```
→ Все кроме product_id_3 будут менять цену

---

## ✅ Что работает сейчас

- ✅ Dashboard с автообновлением (30с)
- ✅ Offers с поиском, контекстным меню, точечным авто
- ✅ Statistics за день/неделю/месяц
- ✅ Новая логика автоизменения: +0.05€
- ✅ Сохранение всех изменений в JSON
- ✅ Защита единственного продавца

---

## 🐛 Что еще нужно добавить

1. **Диалог настроек автоизменения**
   - Сейчас: кнопка "⚙️ Settings" показывает инфо
   - Нужно: полноценный диалог с полями

2. **Массовые операции**
   - Включить автоизменение для выбранных
   - Обновить сток для всех

3. **Графики в статистике**
   - Сейчас: только текст
   - Можно: matplotlib графики

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте `auto_price_settings.json`
2. Проверьте `price_changes_stats.json`
3. Перезапустите приложение
4. Проверьте консоль на ошибки

---

Готово! 🎉 Теперь у вас есть полный контроль над автоизменением цен!
