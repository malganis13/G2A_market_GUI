# 🚀 Quick Start Guide - G2A Market GUI

## 📝 Перед запуском

### 1. Проверьте Python

```bash
python --version
# Должно быть Python 3.8 или выше
```

### 2. Клонируйте репозиторий

```bash
git clone https://github.com/malganis13/G2A_market_GUI.git
cd G2A_market_GUI
git checkout gui-refactor
```

### 3. Создайте виртуальное окружение

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

Если возникают ошибки, установите по одному:

```bash
pip install PyQt6
pip install qasync
pip install httpx
pip install curl-cffi
pip install fastapi
pip install uvicorn
```

### 5. Настройте G2A API

Откройте `g2a_config.py` и заполните:

```python
G2A_CLIENT_ID = "your_client_id_here"
G2A_CLIENT_SECRET = "your_client_secret_here"
```

Или используйте вкладку Settings в GUI.

### 6. Запустите приложение

```bash
python gui_main.py
```

---

## 🎯 Первые шаги

### Шаг 1: Добавьте ключи

1. Перейдите на вкладку **"Keys Management"**
2. Заполните форму:
   - Game Name: `Counter-Strike 2`
   - Key: `XXXXX-XXXXX-XXXXX`
   - Platform: `Steam`
   - Region: `Global`
3. Нажмите **"Add Key"**

**Или импортируйте из файла:**

Создайте `keys.txt`:
```
Counter-Strike 2 | AAAAA-BBBBB-CCCCC | Steam | Global
Cyberpunk 2077 | DDDDD-EEEEE-FFFFF | Steam | Global
```

Нажмите **"Import from File"** и выберите файл.

### Шаг 2: Парсинг цен

1. Перейдите на вкладку **"Price Parsing"**
2. Нажмите **"Select Without Prices"** - выберутся все игры без цен
3. Нажмите **"▶️ Start Parsing"**
4. Наблюдайте за прогрессом в логе

### Шаг 3: Создайте офферы

1. Перейдите на вкладку **"Offers"**
2. Нажмите **"🔄 Load Games"**
3. Выберите игры для создания офферов
4. Нажмите **"🚀 Create Selected Offers"**
5. Наблюдайте за процессом в логе

---

## ⚠️ Решение проблем

### Проблема: Приложение не запускается

**Ошибка:** `ModuleNotFoundError: No module named 'PyQt6'`

**Решение:**
```bash
pip install PyQt6 qasync
```

---

### Проблема: Ошибка базы данных

**Ошибка:** `sqlite3.OperationalError`

**Решение:**
1. Удалите `keys.db`
2. Перезапустите приложение

---

### Проблема: G2A API ошибка 401

**Ошибка:** `401 Unauthorized`

**Решение:**
1. Проверьте `g2a_config.py` - правильные ли Client ID и Secret
2. Перейдите в Settings и перепроверьте ключи
3. Убедитесь, что ваш API ключ активен в G2A Dashboard

---

### Проблема: Парсинг не работает

**Решение:**
1. Проверьте, что файл `price_parser.py` или `parser.py` существует
2. Проверьте логи на вкладке Parsing
3. Увеличьте Delay в Settings

---

## 📚 Дополнительная информация

### Структура файлов

```
G2A_market_GUI/
├── gui_main.py           # Запуск GUI
├── gui/
│   ├── main_window.py    # Главное окно
│   ├── styles.py         # Стили
│   └── tabs/             # Вкладки
├── database.py          # База данных
├── g2a_api_client.py    # G2A API
├── price_parser.py      # Парсер цен
└── requirements.txt     # Зависимости
```

### Горячие клавиши

- **F5** - Обновить все данные
- **Ctrl+Q** - Выход

### Логи

Все логи сохраняются в папке `logs/`

---

## ❓ FAQ

**Q: Можно ли использовать старую базу данных?**

A: Да, GUI совместим с существующей базой `keys.db`

**Q: Можно ли запустить несколько парсингов одновременно?**

A: Нет, парсинг выполняется последовательно

**Q: Как обновить приложение?**

A: 
```bash
git pull origin gui-refactor
pip install -r requirements.txt --upgrade
```

---

## 👍 Полезные ссылки

- [G2A Developer Portal](https://www.g2a.com/developer)
- [PyQt6 Documentation](https://doc.qt.io/qtforpython-6/)
- [Project Repository](https://github.com/malganis13/G2A_market_GUI)

---

🎉 **Приятной работы с GUI приложением!**
